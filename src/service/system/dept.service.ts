import Dept from '@/model/system/dept.model'

class DeptService {
  /**
   * 一级菜单 1开头
   * 二级菜单 100开头
   * 三级菜单 500开头
   * 按钮权限 1000开头
   */
  // 获取菜单
  async getDeptSer(menu_id) {}
}

export const { getDeptSer } = new DeptService()
